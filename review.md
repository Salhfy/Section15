# Laravel Lesson レビュー①

## Todo一覧機能

### Todoモデルのallメソッドで実行しているSQLは何か
"select * from `todos`"を実行し全件取得を行なっている。
### Todoモデルのallメソッドの返り値は何か
Eloquentコレクションのインスタンス　// dd($todo);→App\Todo {#233 ▶}
### 配列の代わりにCollectionクラスを使用するメリットは
短いコードでDBの操作ができ、保守性・可読性の向上につながる
### view関数の第1・第2引数の指定と何をしているか
第一引数は画面に表示したいbladeファイル、第二引数は連想配列
### index.blade.phpの$todos・$todoに代入されているものは何か
＄todosがallメソッドで得た全てのデータ、＄todoが１件分のTodoデータ
## Todo作成機能

### Requestクラスのallメソッドは何をしているか
全件データを連想配列として一括で取得する
### fillメソッドは何をしているか
指定した連想配列のデータをまとめて１度に代入する関数
### $fillableは何のために設定しているか
一括代入の脆弱性を防ぐため。
### saveメソッドで実行しているSQLは何か
insert into `todos` (`content`, `created_at`, `updated_at`)
values ('aa', '2025-09-03 00:00:00', '2025-09-03 00:00:00');

### redirect()->route()は何をしているか
指定したURLにリダイレクトする

## その他

### テーブル構成をマイグレーションファイルで管理するメリット
Gitで情報共有が可能となるので、チーム開発の際データベースのバージョン管理が容易である。
### マイグレーションファイルのup()、down()は何のコマンドを実行した時に呼び出されるのか
artisanコマンドでファイルの作成を行うときにup(),rollbackコマンドで元の状態に戻すときにdownコマンドを実行する
### Seederクラスの役割は何か
データベースに初期データやテストデータを流し込むためのクラス。
### route関数の引数・返り値・使用するメリット
引数がURLパス、返り値はURL文字列、名前付きルートで管理することでパスの変更に柔軟に対応でき、URL管理が簡潔にできる
### @extends・@section・@yieldの関係性とbladeを分割するメリット
@extendsで継承する親Bladeを指定し、@section~@endsectionで囲われた部分を@yieldの部分に挿入している。引数に同じ文字列を使用して@sectionと@yieldを紐づけている。共通する部分を別bladeに分割することで同じ記述を再利用できる
### @csrfは何のための記述か
CSRF攻撃を防ぐためのセキュリティ機能
### {{ }}とは何の省略系か
<?php echo ...; ?>の省略形
